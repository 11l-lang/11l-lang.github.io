[[[H‘T/type’
]]]H‘T/type keyword’


#(11l)‘
T <type name>
   String s // type variable
   F (s) // constructor
      .s = s
   F get_s_len() // type function
      R .s.len
’


H‘T/type subkeywords’


`T`/`type` has subwords `base`, `enum` and `interface`, but they are not supported yet.
