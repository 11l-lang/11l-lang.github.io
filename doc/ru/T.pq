[[[Н‘T/type’
]]]Н‘Ключевое слово T/type’


#(11l)‘
T <имя типа>
   String s // переменная типа
   F (s) // конструктор
      .s = s
   F get_s_len() // функция типа
      R .s.len
’


Н‘Ключевые подслова T/type’


У `T`/`type` есть подслова `base`, `enum` и `interface`, но они пока что не поддерживаются.
